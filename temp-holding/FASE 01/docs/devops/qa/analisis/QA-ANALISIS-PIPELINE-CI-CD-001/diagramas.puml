@startuml
!theme blueprint
actor Developer
participant "Git Hooks" as Hooks
participant "CI local\nscripts/ci-local.sh" as Local
participant "GitHub Actions\nworkflows" as GHA
participant "Artefactos\n(Codecov, reports, docs)" as Artifacts

Developer -> Hooks : pre-commit / pre-push
Hooks --> Developer : valida secretos, naming, entorno
Developer -> Local : scripts/ci-local.sh
Local -> Local : run-all-checks.sh + gates (ruff, pytest, bandit)
Local --> Developer : logs y cobertura local
Developer -> GHA : push / PR
GHA -> GHA : fan-out workflows (python_ci, frontend-ci, infra, docs)
GHA -> Artifacts : coverage.xml, htmlcov, bandit-report.json, documentación
Artifacts -> GHA : quality gates (>=80 % cobertura)
GHA --> Developer : estado de checks y releases
@enduml
